export default {
  staticKeys: ['staticI18n'],

  preTransformNode (el, options) {
    console.log('preTransformNode', el, options)
  },

  transformNode (el, options) {
    console.log('transformNode', el, options, options.delimiters)
  },

  postTransformNode (el, options) {
    console.log('postTransformNode', el, options)
  },

  genData (el) {
    console.log('genData', el)
    return ''
  },

  transformCode (el, code) {
    console.log('transformCode', el, code)
    return code
  }
}
